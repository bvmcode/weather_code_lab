# write a function to calculate the wind speed from the wind direction and wind speed
def calculate_wind_speed(wind_direction, wind_speed):
    return wind_speed

# write a function to calculate the wind speed from the wind direction and wind speed
def calculate_wind_speed(wind_direction, wind_speed):
    return wind_speed

# write a function to calculate the wind speed from the wind direction and wind speed
def calculate_wind_speed(wind_direction, wind_speed):
    return wind_speed

import boto3
def write_to_s3(file_path, bucket_name, file_name):
    s3 = boto3.client('s3')
    s3.upload_file(file_path, bucket_name, file_name)

# write a function to calculate the wind speed from the wind direction and wind speed
def calculate_wind_speed(wind_direction, wind_speed):
    return wind_speed
